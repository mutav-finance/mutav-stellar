# Components
> Design System Analysis | Generated: {DATE}

## UI Kit Detection

**UI Kit:** {e.g., shadcn/ui, RN Reusables, Radix, MUI, none}
**Source:** {how detected — e.g., "Found in package.json dependencies", "Detected components/ui/ directory"}

## Existing Components

| Component | Path | Props / Variants | Reusable? | Notes |
|-----------|------|------------------|-----------|-------|
| {name} | `{path}` | {key props} | {yes/no/partially} | {notes} |

> Capped at 30 components. {N remaining components summarized below if applicable.}

### Additional Components (summary)

{If >30 components: group by directory, list counts per category.}

## Where to Add

| Type | Location | Pattern |
|------|----------|---------|
| UI component | `{path}` | {e.g., PascalCase directory, index.tsx barrel} |
| Page / Screen | `{path}` | {e.g., kebab-case, page.tsx convention} |
| Utility | `{path}` | {e.g., camelCase, named exports} |
| Hook | `{path}` | {e.g., use-prefix, named export} |
