# Palette Generation

Domain expertise for OKLCH palette generation from hex colors.

---

## Modes

| Input | Mode |
|-------|------|
| `#hex [#hex...]` | Direct — parse hex values, assign roles in order |
| `#hex [#hex...] --preview` | Preview — show scales without writing files |

## Color role assignment

Parse hex values from invocation and assign semantic roles in order:
1. First color → **primary**
2. Second color → **secondary**
3. Third color → **accent**
4. Additional colors → **custom-{n}**

Minimum colors: primary. Secondary and accent are recommended but optional.

Color names must be semantic (primary, secondary, accent, neutral) — not literal (red, blue).

## Complement generation

When the user has only a primary and wants suggestions:
- **Secondary:** rotate hue by 180 degrees (complement)
- **Accent:** rotate hue by 30 degrees (analogous)

Present the generated suggestions and let the user confirm or adjust via `AskUserQuestion`.

## OKLCH scale generation via tints.dev API

For each color, call the tints.dev API:

```
WebFetch: https://www.tints.dev/api/{colorName}/{hexWithoutHash}
```

Where:
- `{colorName}` is the semantic role (e.g., `primary`, `secondary`, `accent`)
- `{hexWithoutHash}` is the hex value without the `#` prefix

The API returns an 11-stop OKLCH scale (50-950) with L, C, H values for each stop.

Every palette gets the full 11-stop scale: 50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950.

Palettes are deterministic — same hex input always produces the same OKLCH scales.

## Preview mode (`--preview`)

Display scales without writing files:

```
  /gsp-color preview
  ═══════════════════════════════════════

  primary (#FF5733)
    50   oklch(0.97 0.01 30)    ████
    100  oklch(0.94 0.03 30)    ████
    200  oklch(0.88 0.06 30)    ████
    300  oklch(0.80 0.10 30)    ████
    400  oklch(0.72 0.14 30)    ████
    500  oklch(0.64 0.18 30)    ████  ← source
    600  oklch(0.54 0.16 30)    ████
    700  oklch(0.45 0.14 30)    ████
    800  oklch(0.36 0.11 30)    ████
    900  oklch(0.27 0.08 30)    ████
    950  oklch(0.20 0.05 30)    ████

  secondary (#3366FF)
    ...

  ─────────────────────────────────────
  Run /gsp-color #FF5733 #3366FF to write files.
```

Stop here. Do not write any files.

## Output path resolution

### Within a brand identity
If a brand context exists (`.design/branding/{brand}/`):
- Write to `{BRAND_PATH}/identity/`
- Palette files sit alongside other identity artifacts

### Standalone (no brand context)
- Write to `.design/branding/_palette/`
- Create minimal directory structure

## palettes.json format

If `{OUTPUT_PATH}/palettes.json` already exists, use `AskUserQuestion`: "palettes.json already exists — overwrite with new palette?" with options **Overwrite** and **Cancel**. If cancelled, skip writing.

```json
{
  "$description": "OKLCH 11-stop color palettes generated by /gsp-color",
  "$source": "tints.dev",
  "primary": {
    "source": "#FF5733",
    "50":  { "oklch": "oklch(0.97 0.01 30)", "hex": "#..." },
    "100": { "oklch": "oklch(0.94 0.03 30)", "hex": "#..." },
    "200": { "oklch": "oklch(0.88 0.06 30)", "hex": "#..." },
    "300": { "oklch": "oklch(0.80 0.10 30)", "hex": "#..." },
    "400": { "oklch": "oklch(0.72 0.14 30)", "hex": "#..." },
    "500": { "oklch": "oklch(0.64 0.18 30)", "hex": "#..." },
    "600": { "oklch": "oklch(0.54 0.16 30)", "hex": "#..." },
    "700": { "oklch": "oklch(0.45 0.14 30)", "hex": "#..." },
    "800": { "oklch": "oklch(0.36 0.11 30)", "hex": "#..." },
    "900": { "oklch": "oklch(0.27 0.08 30)", "hex": "#..." },
    "950": { "oklch": "oklch(0.20 0.05 30)", "hex": "#..." }
  },
  "secondary": { "..." : "..." },
  "accent": { "..." : "..." }
}
```

## color-system.md format (palette-only)

Write `{OUTPUT_PATH}/color-system.md` as a foundation chunk per chunk-format.md:

```markdown
# Color System

> Phase: identity | Brand: {name} | Generated: {DATE}

---

## Source Colors

| Role | Hex | OKLCH |
|------|-----|-------|
| Primary | {hex} | {oklch at 500} |
| Secondary | {hex} | {oklch at 500} |
| Accent | {hex} | {oklch at 500} |

## Palette Scales

### Primary

| Stop | OKLCH | Hex | Usage |
|------|-------|-----|-------|
| 50 | {value} | {hex} | Tinted backgrounds |
| 100 | {value} | {hex} | Hover states, borders |
| 200 | {value} | {hex} | Light accents |
| 300 | {value} | {hex} | Decorative elements |
| 400 | {value} | {hex} | Icons, secondary actions |
| 500 | {value} | {hex} | Primary buttons, links |
| 600 | {value} | {hex} | Hover on primary |
| 700 | {value} | {hex} | Active/pressed states |
| 800 | {value} | {hex} | High-contrast text on light |
| 900 | {value} | {hex} | Headings on light backgrounds |
| 950 | {value} | {hex} | Near-black text |

(Repeat for secondary, accent)

## Contrast Notes

- **On white (#fff):** primary-600+ meets WCAG AA for normal text (4.5:1)
- **On black (#000):** primary-400 and below meets WCAG AA for normal text
- **Primary on secondary:** check combination at 500-stops for sufficient contrast
- Use 50/100 stops for backgrounds, 800/900/950 for text on those backgrounds

## Dark Mode Mapping

| Light mode | Dark mode | Rationale |
|------------|-----------|-----------|
| primary-500 (buttons) | primary-400 | Lighter for dark bg contrast |
| primary-50 (bg tint) | primary-950 | Invert the tint |
| primary-900 (text) | primary-100 | Invert for readability |
| secondary-500 | secondary-400 | Same shift as primary |

---

## Related

- [palettes.json](./palettes.json)
- [typography.md](./typography.md)
```

## Completion output

```
  /gsp-color — {n} colors scaled
  ═══════════════════════════════════════

  {OUTPUT_PATH}/
  ├── palettes.json
  └── color-system.md

  Colors: {primary hex}, {secondary hex}, {accent hex}
  Stops: 11 per color (50–950, OKLCH)

  ─────────────────────────────────────
```
